# Generated by pecans-spec. Do not edit.
from __future__ import annotations

from ...runtime import Call, MethodCall, Module
from ..types import FeedResponse, FeedScrollResponse


class FeedModule(Module):
    def start(self, user: Any, size: int = 10) -> Call[FeedResponse]:
        """Open a feed and return its first page.

        Args:
            user: Username to fetch the activity feed for, or `null` for the global site feed.
            size: Number of items to return on this first page.

        Returns:
            A cursor (`feedId`) and the first page of items.
        """
        return Call(
            self.client,
            MethodCall(
                fn="feed.start",
                payload={
                    "user": user,
                    "size": size,
                },
            ),
        )

    def scroll(self, feedId: str, numItems: int, lastId: int) -> Call[FeedScrollResponse]:
        """Fetch the next page of a feed opened with `start`.

        Args:
            feedId: Cursor returned by `start`.
            numItems: Number of further items to fetch.
            lastId: Id of the last item you've already seen.

        Returns:
            The next page of items, or `end: true` once exhausted.
        """
        return Call(
            self.client,
            MethodCall(
                fn="feed.scroll",
                payload={
                    "feedId": feedId,
                    "numItems": numItems,
                    "lastId": lastId,
                },
            ),
        )
