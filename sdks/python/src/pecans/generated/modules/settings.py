# Generated by pecans-spec. Do not edit.
from __future__ import annotations

from ...runtime import Call, MethodCall, Module
from ..types import SettingsGetResponse, TCResponse


class SettingsModule(Module):
    def get(self) -> Call[SettingsGetResponse]:
        """Fetch the current account settings.

        Returns:
            The account's email and message preferences.
        """
        return Call(
            self.client,
            MethodCall(
                fn="legacy.settingsget",
                payload={},
            ),
        )

    def set(
        self, email: str, msgNotify: bool, msgHideAnon: bool, pwOld: str = "", pwNew: str = ""
    ) -> Call[TCResponse]:
        """Save account settings. Sends the full settings each time, so pass the current
        values for anything you're not changing. Leave the password fields empty to
        keep the current password.

        Args:
            email: Account email address.
            msgNotify: Whether to notify on new private messages.
            msgHideAnon: Whether to hide sender identity on anonymous messages.
            pwOld: Current password, required only when changing it.
            pwNew: New password, when changing it.
        """
        return Call(
            self.client,
            MethodCall(
                fn="legacy.settingsset",
                payload={
                    "email": email,
                    "msgNotify": msgNotify,
                    "msgHideAnon": msgHideAnon,
                    "pwOld": pwOld,
                    "pwNew1": pwNew,
                    "pwNew2": pwNew,
                },
            ),
        )
