/* Generated by pecans-spec. Do not edit. */
import { Call, Module } from "../../runtime";
import { QaGetPinnedResponse } from "../types";

export class QaModule extends Module {
    /**
     * List the questions a user has pinned to their profile.
     *
     * @param profileId - Username whose pinned questions to fetch.
     * @returns The user's pinned questions.
     */
    public getPinned(profileId: string): Call<QaGetPinnedResponse> {
        return new Call<QaGetPinnedResponse>(this.client, {
            fn: "qa.getpinned",
            payload: {
                "profileId": profileId,
            },
        });
    }
}
