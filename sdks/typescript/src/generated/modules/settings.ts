/* Generated by pecans-spec. Do not edit. */
import { Call, Module } from "../../runtime";
import { SettingsGetResponse, TCResponse } from "../types";

export class SettingsModule extends Module {
    /**
     * Fetch the current account settings.
     *
     * @returns The account's email and message preferences.
     */
    public get(): Call<SettingsGetResponse> {
        return new Call<SettingsGetResponse>(this.client, {
            fn: "legacy.settingsget",
            payload: {},
        });
    }

    /**
     * Save account settings. Sends the full settings each time, so pass the current
     * values for anything you're not changing. Leave the password fields empty to
     * keep the current password.
     *
     * @param email - Account email address.
     * @param msgNotify - Whether to notify on new private messages.
     * @param msgHideAnon - Whether to hide sender identity on anonymous messages.
     * @param pwOld - Current password, required only when changing it.
     * @param pwNew - New password, when changing it.
     */
    public set(
        email: string,
        msgNotify: boolean,
        msgHideAnon: boolean,
        pwOld: string = "",
        pwNew: string = "",
    ): Call<TCResponse> {
        return new Call<TCResponse>(this.client, {
            fn: "legacy.settingsset",
            payload: {
                email: email,
                msgNotify: msgNotify,
                msgHideAnon: msgHideAnon,
                pwOld: pwOld,
                pwNew1: pwNew,
                pwNew2: pwNew,
            },
        });
    }
}
