{
  "name": "pecans-monorepo",
  "version": "1.0.0",
  "description": "Pecans Two Cans & String API wrapper and MCP server",
  "private": true,
  "license": "GPL-3.0-only",
  "workspaces": [
    "packages/*",
    "sdks/typescript"
  ],
  "scripts": {
    "build": "npm run build --workspace pecans && npm run build --workspace pecans-mcp",
    "build:lib": "npm run build --workspace pecans",
    "build:mcp": "npm run build --workspace pecans-mcp",
    "clean": "npm run clean --workspace pecans"
  }
}
